{
  "name": "rock-paper-scissors-extended",
  "version": "1.0.0",
  "packageManager": "pnpm@8.5.1",
  "description": "",
  "author": "RichGihratik (Cyrus)",
  "license": "MIT",
  "scripts": {
    "build-test": "npm run build && npm run run",
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rm -rf dist build",
    "run": "node dist Rock Scissors Paper"
  },
  "devDependencies": {
    "@types/node": "^20.2.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@types/terminal-kit": "^2.5.1",
    "terminal-kit": "^3.0.0"
  }
}
